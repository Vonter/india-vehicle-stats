// Disable server-side rendering for the entire app
export const ssr = false;

// Enable client-side routing
export const csr = true;

// This makes the app a true SPA
export const prerender = false;
